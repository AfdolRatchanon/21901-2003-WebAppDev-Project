# Frontend Environment Variables
# Copy ไฟล์นี้เป็น .env แล้วแก้ค่าให้ตรง

# URL ของ Backend API
# Development: ปล่อยว่างเปล่า (Vite proxy จะจัดการให้)
# Production: ใส่ URL จาก Railway
VITE_API_URL=
