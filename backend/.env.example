# Backend Environment Variables
# Copy ไฟล์นี้เป็น .env แล้วแก้ค่า

# Database
# Development (XAMPP MySQL):
DATABASE_URL="mysql://root:@localhost:3306/equipment_db"
# Production (PostgreSQL — ค่าจาก Railway):
# DATABASE_URL="postgresql://user:password@host:5432/dbname"

# JWT Secret — ต้องเปลี่ยนเป็นค่าที่ซับซ้อนใน Production!
JWT_SECRET="change-this-to-a-very-long-random-secret-key"

# Port
PORT=3000

# Frontend URL (สำหรับ CORS)
# Development:
FRONTEND_URL="http://localhost:5173"
# Production (ค่าจาก Vercel):
# FRONTEND_URL="https://your-app.vercel.app"
